#pragma once

typedef unsigned short ushort;

namespace mcpp
{
namespace utils
{

typedef unsigned char byte; // TODO: Make this a signed char, because unsigned bytes and normal
                            // bytes aren't the same...

} // namespace utils
} // namespace mcpp
